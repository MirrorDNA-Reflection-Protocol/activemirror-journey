{
  "audit_log_request": {
    "schema_version": "audit_log_request.v0_1",
    "id": "audit_source_adapter_invocation_contract",
    "task_id": "active_mirror_front_door_v1",
    "actor": "codex",
    "action": "Record the disabled AMOS source adapter invocation contract before any invocation is wired.",
    "checked_scope": [
      "source adapter invocation contract request schema",
      "source adapter invocation contract receipt schema",
      "source adapter invocation contract gate",
      "HomePage.jsx import scan",
      "HomePage.jsx invocation scan",
      "disabled adapter invariant scan",
      "blocked live invocation request"
    ],
    "unchecked_scope": [
      "actual adapter invocation",
      "live app UI integration",
      "gateway adapter",
      "model calls",
      "network calls",
      "durable memory writes",
      "route changes",
      "public deploy"
    ],
    "evidence": [
      "npm run guard:source-adapter-invocation-contract",
      "node scripts/amos_source_adapter_invocation_contract_gate.mjs --write"
    ],
    "bad_news": [
      "The invocation contract is declared, but the adapter is still intentionally not invoked.",
      "This is still not live AMOS runtime enforcement in the public app."
    ],
    "decision": "partial",
    "follow_up": [
      "Only add an invocation after a separate gate proves the call site, input receipt, and output projection."
    ]
  }
}
