{
  "audit_log_request": {
    "schema_version": "audit_log_request.v0_1",
    "id": "amos_browser_runtime_adapter",
    "action": "Verify the AMOS browser-local runtime adapter proposal emits an in-memory request projection without live action.",
    "checked_scope": [
      "browser runtime adapter request schema",
      "browser runtime adapter receipt schema",
      "browser runtime adapter gate",
      "runtime projection receipt",
      "blocked live request"
    ],
    "unchecked_scope": [
      "live app adapter",
      "gateway adapter",
      "model calls",
      "network calls",
      "durable memory writes",
      "route changes",
      "public deploy"
    ],
    "evidence": [
      "npm run guard:browser-runtime-adapter",
      "node scripts/amos_browser_runtime_adapter_gate.mjs --write"
    ],
    "bad_news": [
      "The browser-local runtime adapter proposal performs no live runtime action and does not prove public app or gateway enforcement."
    ],
    "decision": "partial",
    "follow_up": [
      "Only after browser-local receipts remain stable, consider wiring a local-only UI harness behind explicit gates."
    ]
  }
}
