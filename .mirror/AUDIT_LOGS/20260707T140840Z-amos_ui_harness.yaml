audit_log:
  id: "audit_20260707t140840z_amos_ui_harness"
  task_id: "active_mirror_front_door_v1"
  created_at: "2026-07-07T14:08:40Z"
  actor: "codex"
  action: "Verify the AMOS local UI harness proposal can call the browser-local runtime adapter without live action."
  checked_scope:
    - "ui harness request schema"
    - "ui harness receipt schema"
    - "ui harness gate"
    - "browser-local runtime adapter call"
    - "blocked live UI request"
  unchecked_scope:
    - "live app UI integration"
    - "gateway adapter"
    - "model calls"
    - "network calls"
    - "durable memory writes"
    - "route changes"
    - "public deploy"
  evidence:
    - "npm run guard:ui-harness"
    - "node scripts/amos_ui_harness_gate.mjs --write"
  bad_news:
    - "The local UI harness proposal performs no live runtime action and does not prove public app or gateway enforcement."
  decision: "partial"
  follow_up:
    - "Only after local UI harness receipts remain stable, consider a disabled source adapter in app code behind explicit gates."
